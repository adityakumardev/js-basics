// execution context & call stack

// [{}] -> Global Execution Context (this)
// value of .this in browser is window object
// Javascript is a single threaded language which works on a process

// Functional Execution Context
// Eval Execution Context

// [{}] -> memory creation phase (memory allocation of variables and objects)
// => execution phase (execution of code line by line)

let val1 = 10
let val2 = 5
function addNum(num1, num2) {
    return total
}
let result1 = addNum(val1, val2)
let result2 = addNum(10, 2)

