console.log("2" == 2) // (true) // == only checks for matching values without considering the datatype, conversion exists if both are different in type

console.log("2" === 2) // (false) // condition will be true only if both its types and values are same
